export enum Routes {
  AUTH = '/auth',
  USERS = '/users',
  ADMIN = '/admin',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USER = 'USER_SERVICE',
  LOGGING = 'LOGGING_SERVICE',
}

export enum SortDirections {
  DESC = 'desc',
  ASC = 'asc',
}
