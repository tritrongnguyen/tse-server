export enum QuestionType {
  NORMAL = 0,
  POLL = 1,
  POLL_MULTIPLE = 2,
  ANNOUNCEMENT = 3,
}

export enum QuestionStatus {
  OPENED = 0,
  LOCKED = 1,
  CLOSED = 2,
}

export enum QuestionTrend {
  NORMAL = 0,
  NEW = 1,
  HOT = 2,
}
