export enum ActivityType {
  PROCUREMENT = 'PROCUREMENT',
  CONTEST = 'CONTEST',
  SEMINAR = 'SEMINAR',
  TRAINING = 'TRAINING',
}

export enum ActivityScope {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL',
}

export enum ActivityStatus {
  PLAN = 'PLAN',
  OPEN = 'OPEN',
  CLOSE = 'CLOSE',
  CANCEL = 'CANCEL',
}
