export enum ActivityType {
  CONTEST = 'CONTEST',
  SEMINAR = 'SEMINAR',
  TRAINING = 'TRAINING',
}

export enum ActivityScope {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL',
}

export enum ActivityStatus {
  IN_COMING = 'IN_COMING',
  OPEN_NOW = 'OPEN_NOW',
  CLOSED = 'CLOSED',
  CANCELED = 'CANCELED',
  FINISHED = 'FINISHED',
}

export enum VenueTypes {
  ONLINE = 'ONLINE',
  OFFLINE = 'OFFLINE',
}
