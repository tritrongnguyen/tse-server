export enum ActivityType {
  PROCUREMENT = 'PROCUREMENT',
  CONTEST = 'CONTEST',
  SEMINAR = 'SEMINAR',
  TRAINING = 'TRAINING',
}

export enum ActivityScope {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL',
}
