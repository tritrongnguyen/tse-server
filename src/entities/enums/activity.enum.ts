export enum ActivityType {
  CONTEST = 'CONTEST',
  SEMINAR = 'SEMINAR',
  TRAINING = 'TRAINING',
}

export enum ActivityScope {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL',
}

export enum ActivityStatus {
  PLANED = 'PLANED',
  OPENED = 'OPENED',
  CLOSED = 'CLOSED',
  CANCELED = 'CANCELED',
}

export enum VenueTypes {
  ONLINE = 'ONLINE',
  OFFLINE = 'OFFLINE',
}
